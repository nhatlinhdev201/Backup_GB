#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@ui-kitten/metro-config/bin/ui-kitten" "$@"
  ret=$?
else 
  node  "$basedir/../@ui-kitten/metro-config/bin/ui-kitten" "$@"
  ret=$?
fi
exit $ret
