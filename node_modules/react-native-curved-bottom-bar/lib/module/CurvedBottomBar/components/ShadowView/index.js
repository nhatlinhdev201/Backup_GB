import { requireNativeComponent, UIManager, Platform, View } from 'react-native';
const LINKING_ERROR = `The package 'react-native-curved-bottom-bar' doesn't seem to be linked. Make sure: \n\n` + Platform.select({
  ios: "- You have run 'pod install'\n",
  default: ''
}) + '- You rebuilt the app after installing the package\n' + '- You are not using Expo Go\n';
const ComponentName = 'CurvedBottomBarView';
export const CurvedBottomBarView = Platform.OS === 'android' ? UIManager.getViewManagerConfig(ComponentName) != null ? requireNativeComponent(ComponentName) : () => {
  throw new Error(LINKING_ERROR);
} : View;
//# sourceMappingURL=index.js.map